name: Finalize PWA power controls v2
on:
  push:
    branches: [feature/power-controls-v2]
permissions:
  contents: write
jobs:
  finalize:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/power-controls-v2
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Apply final recording/power-state handling
        run: node tools/finalize-power-controls-v2.cjs
      - name: Validate syntax and regressions
        run: |
          node --check app.js
          node --check power-controls.js
          node --check recording-bridge.js
          node --check theme.js
          node --check sw.js
          for test_file in tests/*.cjs; do node "$test_file"; done
      - name: Commit transformed sources
        run: |
          if git diff --quiet; then
            echo "Final power-state handling already materialized."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app.js tests/power-controls-v2.cjs
          git commit -m "Preserve Deep Sleep and Standby through finalization"
          git push origin HEAD:feature/power-controls-v2
